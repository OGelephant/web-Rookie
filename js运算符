Infintiy属于number类型
###比较运算符
结果是布尔类型true和false
字符串比较的是asc码的顺序
10在字符串中不读十，读10.
NaN不等于自己，不等于任何数。

###逻辑运算符
与运算符&&
先看第一个表达式转换成布尔值的结果，如果结果为真，那么它会看第二个表达式转换为布尔值的结果，如果只有两个表达式的话，只看第二个表达式就可以返回该表达式的值。
若第一个表达式的布尔值为false那么不用看后面的表达式直接将第一个表达式的值返回即可。
// undefined,null,NaN,"",0,false 他们的布尔值都为false.
||可以用来写兼容
